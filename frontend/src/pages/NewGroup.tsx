import React, { useEffect, useState } from "react";
import { Box, Text, Button, Flex, Heading, FormControl, FormLabel } from "@chakra-ui/react";

const NewGroup: React.FC = () => {
    return (
        <Box>
            <Text> New Group Page </Text>
        </Box>
    );
};

export default NewGroup;